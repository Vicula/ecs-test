// by convention, composable function names start with "use"
export default class useLoading {

  // expose managed state as return value through constructor
  constructor(isLoading: boolean, debounceTime: number) {
   
  }

}
